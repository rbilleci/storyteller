[pytest]
testpaths = tests_narrator
asyncio_mode = auto
addopts = -m "not live"
markers =
    live: reaches the served model or a live network endpoint
filterwarnings =
    ignore::DeprecationWarning
