"""The Black Sword Hack narrator runtime.

Modules that import Strands: ``engine``. Everything else is standard library only, so
the main test environment can pin the security policy, the leak filter, prompt assembly
and the channel contract without installing the framework.
"""
